export enum TimerStatuses {
    idle = 'idle',
    running = 'running',
    paused = 'paused',
    resumed = 'resumed',
}

export enum TimerDirections {
    forward = 'forward',
    backward = 'backward',
}
